export type AssetModel = {
  id: string;
  projectId: string;
  assetName: string;
  price: number;
  purchaseDate: string;
  quantity: number;
  assetStatus: number; // TODO status
  serialNumber: string;
}
